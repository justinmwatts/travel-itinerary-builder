import { PlaceholderScreen } from "../../components/PlaceholderScreen";

// Protected. The two-panel chat builder arrives in Phase 3.
export function BuilderPage() {
  return (
    <PlaceholderScreen
      title="Builder"
      description="The conversational itinerary builder lands in Phase 3. Reaching this screen confirms the auth guard let you through."
    />
  );
}
