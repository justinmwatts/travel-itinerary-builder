import { PlaceholderScreen } from "../../components/PlaceholderScreen";

// Public landing. The real searchable, reactable grid arrives in Phase 8.
export function FeedPage() {
  return (
    <PlaceholderScreen
      title="Feed"
      description="The public feed of published itineraries lands in Phase 8. For now, sign in to reach the protected screens."
    />
  );
}
